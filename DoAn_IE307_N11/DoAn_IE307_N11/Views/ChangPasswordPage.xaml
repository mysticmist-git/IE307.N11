<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DoAn_IE307_N11.Views.ChangPasswordPage"
             Background="{StaticResource Background}"
             Title="Thay đổi mật khẩu">
    <ContentPage.Content>
        <StackLayout Padding="10">
            <Entry IsPassword="True" Placeholder="Mật khẩu cũ" BackgroundColor="{StaticResource Background}"/>
            <Entry IsPassword="True" Placeholder="Mật khẩu mới" BackgroundColor="{StaticResource Background}"/>
            <Entry IsPassword="True" Placeholder="Nhập lại mật khẩu mới" BackgroundColor="{StaticResource Background}"/>

            <Button Text="Thay đổi mật khẩu"
                    TextColor="{StaticResource Background}"
                    BackgroundColor="{StaticResource Primary}"/>

            <Button Text="Quên mật khẩu?"
                    TextColor="{StaticResource Primary}"
                    BackgroundColor="{StaticResource Background}"
                    x:Name="BtnChangePassword"
                    Clicked="BtnChangePassword_Clicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>